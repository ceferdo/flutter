import 'package:flutter/material.dart';

import '../main.dart';

class Texto extends StatelessWidget {
  var meuvalor = valor;
  Widget build(BuildContext context) {
    return Text('Ola flutter', style: TextStyle(color: Colors.amber));
  }
}
